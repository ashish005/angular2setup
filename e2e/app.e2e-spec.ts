import { ApiPortalAppPage } from './app.po';

/*describe('api-portal-app App', function() {
  let page: ApiPortalAppPage;

  beforeEach(() => {
    page = new ApiPortalAppPage();
  });

  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });
});*/
